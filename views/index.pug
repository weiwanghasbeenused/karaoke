doctype html
html
    include includes/head.pug
    body
        main.full-vh(prompt=''+prompt)
            div#booker-container
        script(src="/js/KaraokeBooker.js")
        script(src="/js/KaraokeRemoteBooker.js")
        script.
            /* websocket */
            const k_booker = new KaraokeRemoteBooker(document.querySelector('#booker-container'), '!{host}', '!{keyword}', '!{socket_timeout}');   
        include includes/foot.pug 